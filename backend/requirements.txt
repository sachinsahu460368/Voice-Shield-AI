# VoiceShield-AI Backend Dependencies
# Python 3.11 recommended

# --- Web framework ---

fastapi==0.115.12
uvicorn==0.34.2
gunicorn==23.0.0
python-multipart==0.0.20

# --- Audio processing ---
librosa==0.10.2.post1
soundfile==0.13.1
numpy>=1.26.0,<2.0
imageio-ffmpeg==0.6.0

# --- ML inference ---
# Torch is installed separately in the Render Build Command
# to guarantee the CPU-only build.
# torch==2.9.0+cpu
